# ~/.bashrc.emacs
# $Id$

. ${HOME}/.default/sh

#
for i in ${SHCONFDIR}/bashrc.emacs.d/*.sh; do
    if [ -x $i ]; then
	. $i ${CONFDOMAIN}
    fi
done
unset i

# Local Variables:
# mode: shell-script
# End:
